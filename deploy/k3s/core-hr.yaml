apiVersion: apps/v1
kind: Deployment
metadata:
  name: core-hr
  namespace: smarthr360
  labels: { app: core-hr }
spec:
  replicas: 1
  selector:
    matchLabels: { app: core-hr }
  template:
    metadata:
      labels: { app: core-hr }
    spec:
      containers:
        - name: core-hr
          image: ghcr.io/nawfalrazouk7/smarthr360-core-hr:main
          ports:
            - containerPort: 8000
          env:
            - name: ALLOWED_HOSTS
              value: "core-hr,api.smarthr360.example.com"
            - name: SECRET_KEY
              valueFrom:
                secretKeyRef: { name: app-secrets, key: CORE_HR_SECRET_KEY }
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef: { name: app-secrets, key: POSTGRES_PASSWORD }
            - name: DATABASE_URL
              value: "postgres://smarthr:$(POSTGRES_PASSWORD)@postgres:5432/smarthr_core_hr"
            - name: SMARTHR_JWT_PUBLIC_KEY_FILE
              value: /keys/jwt_public.pem
            - name: SMARTHR_JWT_ISSUER
              value: smarthr360
          volumeMounts:
            - name: jwt-public
              mountPath: /keys
              readOnly: true
          resources:
            requests: { cpu: 100m, memory: 192Mi }
            limits: { cpu: 500m, memory: 384Mi }
          readinessProbe:
            httpGet: { path: /healthz/, port: 8000 }
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /healthz/, port: 8000 }
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: jwt-public
          secret:
            secretName: jwt-keys
            items:
              - key: jwt_public.pem
                path: jwt_public.pem
---
apiVersion: v1
kind: Service
metadata:
  name: core-hr
  namespace: smarthr360
spec:
  selector: { app: core-hr }
  ports:
    - port: 8000
